// datasource db {
//   provider = "postgresql"
//   url      = "postgresql://postgres:example@localhost:5432/my_first_app"
// }

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    Customer?   @relation(fields: [authorId], references: [id])
  authorId  Int?
}

model Customer {
  id    Int     @id @default(autoincrement())
  name  String
  age Int
  email String  @unique
  is_active Boolean
  posts Post[]
}